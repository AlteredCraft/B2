{
  "name": "b2-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "B2 desktop frontend — the presentation layer over the Tauri host (specs/desktop-ui-mvp.md §2). Vanilla TS + Vite; talks to b2-core only through the b2-desktop IPC commands, all via the one seam src/api.ts.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
