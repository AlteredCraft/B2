{
  "description": "Hand-labelled connection-discovery set (the eval harness, crates/b2-embed/evals/): for each anchor note, the corpus notes a human would say belong next to it — its topic-cluster mates. Scores `Vault::similar` (discover::candidates — the two-stage centroid shortlist + exact max-sim, GH #38), which the retrieval eval alone does NOT cover: note-to-note discovery is a different task from query-to-note search even over the same stored vectors. A hit = any `expected` path in the top candidates. An EMPTY `expected` marks a NEGATIVE anchor — a deliberate loner whose labelled answer is that nothing in the corpus relates — scoring suppression (does discovery say so, or serve strangers?) rather than rank; every candidate surfaced for one is junk by label (index-engine.md §3's quality-floor ruling, PR #145). A loner must be orthogonal to every topic cluster, to all three long distractor notes, AND to the other loners, or the label is arguable — which is why the loners are watercolor / chess / laundry and deliberately NOT money-adjacent (personal-finance.md is in the corpus). The corpus is unlinked, so no candidate is excluded by the 1-hop graph rule.",
  "anchors": [
    { "anchor": "espresso.md", "expected": ["french-press.md", "coffee-roasting.md"] },
    { "anchor": "insomnia.md", "expected": ["sleep-hygiene.md", "dreaming.md"] },
    { "anchor": "photosynthesis.md", "expected": ["plant-roots.md", "houseplant-care.md"] },
    { "anchor": "encryption.md", "expected": ["passwords.md", "phishing.md"] },
    { "anchor": "volcano.md", "expected": ["earthquakes.md", "glaciers.md"] },
    { "anchor": "bicycle.md", "expected": ["bike-maintenance.md"] },

    { "anchor": "watercolor-painting.md", "expected": [] },
    { "anchor": "chess-openings.md", "expected": [] },
    { "anchor": "stain-removal.md", "expected": [] }
  ]
}
