---
type: note
title: HTTP/2
---
A revision of HTTP that multiplexes many concurrent streams over a single TCP connection, so a slow response no longer blocks the others. It keeps HTTP/1.1's methods and semantics but replaces the wire format, removing the need for the parallel-connection workarounds it was designed to end.
